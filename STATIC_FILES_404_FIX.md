# 静的ファイル404エラー修正ガイド

**問題**: `/admin/dashboard`ページでCSS/JSファイルが404エラー  
**確認日**: 2026-01-03  
**状態**: ⚠️ **原因特定・修正手順作成**

---

## 🔍 問題の詳細

### エラーメッセージ

1. **CSSファイル404**:
   ```
   Failed to load resource: 404
   a1e4600e01d42049.css
   ```

2. **JavaScriptファイル404**:
   ```
   Failed to load resource: 404
   page-2b1778508c47e43f.js
   ```

3. **ChunkLoadError**:
   ```
   ChunkLoadError: Loading chunk 427 failed
   /next/static/chunks/app/admin/dashboard/page-2b1778508c47e43f.js
   ```

---

## 🔍 原因分析

### 現在の設定

1. **next.config.js**: `output: 'standalone'`
2. **package.json**: `start: "node .next/standalone/server.js"`
3. **render.yaml**: `buildCommand: npm install && npm run build`

### 問題の可能性

#### 可能性1: ビルドが実行されていない

`render.yaml`には`buildCommand: npm install && npm run build`が設定されていますが、実際にビルドが実行されていない可能性があります。

**確認方法**:
- Render Dashboardでデプロイログを確認
- `npm run build`が実行されているか確認
- `.next`ディレクトリが生成されているか確認

#### 可能性2: standaloneビルドの静的ファイルパス問題

standaloneビルドでは、静的ファイルは`.next/static/`に配置されますが、サーバー側で正しく配信する必要があります。

**問題点**:
- Next.jsが生成する`.next/standalone/server.js`が静的ファイルを正しく配信していない可能性
- パスの設定が間違っている可能性

---

## ✅ 解決方法

### 方法1: Render Dashboardでビルドログを確認（最優先）

1. **Render Dashboardにログイン**
   - https://dashboard.render.com

2. **サービスを選択**
   - `fleapay-lite-t1` または `fleapay-lite-web`

3. **Logsタブを開く**
   - 最新のデプロイログを確認

4. **確認ポイント**:
   ```
   ==> Running build command 'npm install && npm run build'...
   > next build
   ✓ Compiled successfully
   ```

5. **ビルドが実行されていない場合**:
   - Render Dashboardの**Settings**タブを開く
   - **Build Command**を確認
   - `npm install && npm run build`に設定されているか確認
   - 設定されていない場合は設定して**Save Changes**

### 方法2: next.config.jsの設定を確認

standaloneビルドで静的ファイルを正しく配信するため、`next.config.js`を確認：

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  
  // 静的ファイルの配信設定（必要に応じて追加）
  // assetPrefix: process.env.NODE_ENV === 'production' ? '' : undefined,
  
  env: {
    // 既存の環境変数をNext.jsでも使用可能にする
  },
};

export default nextConfig;
```

### 方法3: 再デプロイを実行

1. **Render DashboardでManual Deploy**
   - **Manual Deploy** → **Deploy latest commit**をクリック

2. **デプロイログで確認**
   - `npm run build`が実行されているか確認
   - ビルドが成功しているか確認
   - `.next`ディレクトリが生成されているか確認

3. **動作確認**
   - `/admin/dashboard`ページにアクセス
   - 静的ファイルが正しく読み込まれるか確認
   - コンソールエラーが解消されたか確認

---

## 📋 確認チェックリスト

- [ ] Render Dashboardでデプロイログを確認
- [ ] `npm run build`が実行されているか確認
- [ ] `.next`ディレクトリが生成されているか確認
- [ ] ビルドエラーが発生していないか確認
- [ ] `next.config.js`の設定を確認
- [ ] Render DashboardでbuildCommandを確認・設定
- [ ] 再デプロイを実行
- [ ] `/admin/dashboard`ページで動作確認
- [ ] 静的ファイルが正しく読み込まれるか確認
- [ ] コンソールエラーが解消されたか確認

---

## 🚀 推奨される対応手順

### ステップ1: Render Dashboardでログを確認

1. Render Dashboardにログイン
2. `fleapay-lite-t1`サービスを選択
3. **Logs**タブを開く
4. 最新のデプロイログを確認
5. `npm run build`が実行されているか確認

### ステップ2: 必要に応じて設定を修正

1. **Render DashboardでbuildCommandを確認**
   - **Settings**タブを開く
   - **Build Command**を確認
   - `npm install && npm run build`に設定されているか確認

2. **設定されていない場合は設定**
   - **Build Command**に`npm install && npm run build`を入力
   - **Save Changes**をクリック

### ステップ3: 再デプロイを実行

1. **Manual Deployを実行**
   - **Manual Deploy** → **Deploy latest commit**をクリック

2. **デプロイログで確認**
   - ビルドが実行されているか確認
   - ビルドが成功しているか確認

### ステップ4: 動作確認

1. `/admin/dashboard`ページにアクセス
2. ブラウザの開発者ツールでコンソールを確認
3. 静的ファイルが正しく読み込まれるか確認
4. エラーが解消されたか確認

---

## 📝 参考情報

- **Next.js Standalone Output**: https://nextjs.org/docs/pages/api-reference/next-config-js/output
- **Render Dashboard**: https://dashboard.render.com
- **検証環境URL**: https://fleapay-lite-t1.onrender.com

