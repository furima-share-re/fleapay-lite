# Git LFS tracking for large database files
# マイグレーションファイルはGit LFSで追跡しない（小さいファイルのため）
supabase/migrations/*.sql !filter !diff !merge -text
# その他のSQLファイルはGit LFSで追跡（大きなバックアップファイルなど）
*.sql filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
tmp/**/*.dat filter=lfs diff=lfs merge=lfs -text





